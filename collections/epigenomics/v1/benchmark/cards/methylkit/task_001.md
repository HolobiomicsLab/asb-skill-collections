# SciTask Card: Reproduce differential methylation analysis using calculateDiffMeth on example CpG files

- Task ID: `task_001`
- Schema version: `0.18.0`
- Created at: `2026-06-15T18:05:47.772040+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_epigenomics/coll_methylkit/synthesized_package`
- Domain: `bioinformatics`
- Subtask categories: `data-processing`, `statistical-analysis`
- GitHub: `al2na/methylKit`
- Quality: Score 2/5 — Coherent: false, placeholder, 6 grounding failures

## Classification

- Task kind: `reproduction`
- Article type: `software-tool`
- Primary domain: `genomics`
- Subdomains: `gene-regulation`
- Techniques: `differential-abundance-analysis`, `statistical-analysis`

## Research Question
When differentially methylated bases are extracted from a methylDiff object using getMethylDiff() with q-value < 0.01 and 25% methylation difference thresholds, what are the counts of hyper-methylated versus hypo-methylated bases?

## Connected Finding
Using getMethylDiff() with q-value < 0.01 and 25% methylation difference cutoffs on example methylKit data yields separate hyper-methylated and hypo-methylated base objects, which can be extracted by specifying type="hyper" or type="hypo" parameters.

## Task Description
Merge methylKit CpG samples using unite(), calculate differential methylation with calculateDiffMeth(), and extract differentially methylated bases meeting q-value < 0.01 and ≥25% methylation difference thresholds using getMethylDiff(). Output a methylDiff object with hyper- and hypo-methylated base counts.

## Inputs
- methylKit example CpG text files (methylation call format from bisulfite sequencing)

## Expected Outputs
- methylDiff object containing differentially methylated bases with hyper- and hypo-methylated base counts

## Landmark Outputs

- `methylRawList.rds`
- `methylBase_united.rds`
- `methylDiff_raw.rds`

## Tools
- methylKit
- R

## Skills
- dna-methylation-differential-analysis
- bisulfite-sequencing-data-processing
- cpg-base-filtering-by-statistical-threshold
- methylation-object-merging-and-union
- methylation-hyper-hypo-classification

## Workflow Description
1. Load methylation call files using methRead() to generate methylRawList objects from example CpG text files. 2. Merge samples across all bases with unite() function to create a methylBase object containing only bases covered in all samples. 3. Calculate differential methylation using calculateDiffMeth() function, which automatically selects Fisher's exact test or logistic regression based on sample size. 4. Extract differentially methylated bases using getMethylDiff() with q-value threshold of 0.01 and percent methylation difference cutoff of 25%, producing a methylDiff object. 5. Validate output by confirming hyper-methylated and hypo-methylated base counts match vignette-reported values.

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `figures/methylKit_logo.png` | figure | False |
| `paper.md` | main_article | True |

## Missing Information
- No changelog found

## Domain Knowledge
- CpG methylation in vertebrates occurs at cytosine-guanine dinucleotides and is quantified as a percentage rather than binary score, reflecting sample heterogeneity and sequencing error.
- Hypo-methylation on regulatory regions (promoters) typically associates with gene transcription, while hyper-methylation associates with gene repression.
- The unite() function merges samples by selecting only base-pair locations covered in all input samples, creating a unified methylBase object for comparative analysis.
- q-value < 0.01 represents a stringent false-discovery-rate threshold; 25% methylation difference threshold identifies biologically meaningful changes in methylation status between sample groups.

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [results] When differentially methylated bases are extracted from a methylDiff object using getMethylDiff() with q-value < 0.01 and 25% methylation difference thresholds, what are the counts of hyper-methylated versus hypo-methylated bases?: 'After q-value calculation, we can select the differentially methylated regions/bases based on q-value and percent methylation difference cutoffs. Following bit selects the bases that have'
- `ev_002` from `agent2_synthesis` (agent2_traced): [results] Using getMethylDiff() with q-value < 0.01 and 25% methylation difference cutoffs on example methylKit data yields separate hyper-methylated and hypo-methylated base objects, which can be extracted by specifying type="hyper" or type="hypo" parameters.: 'myDiff25p.hyper=getMethylDiff(myDiff,difference=25,qvalue=0.01,type="hyper")
#
# get hypo methylated bases
myDiff25p.hypo=getMethylDiff(myDiff,difference=25,qvalue=0.01,type="hypo")'
- `ev_003` from `agent2_synthesis` (agent2_traced): [methods] methylKit example CpG text files (methylation call format from bisulfite sequencing): 'Starting from the methylKit example CpG text files'
- `ev_004` from `agent2_synthesis` (agent2_traced): [methods] methylDiff object containing differentially methylated bases with hyper- and hypo-methylated base counts: 'methylDiff object with hyper- and hypo-methylated base counts matching those reported in the vignette'
- `ev_005` from `agent2_synthesis` (agent2_traced): [methods] methylKit: 'title: "methylKit: User Guide v`r packageVersion('methylKit')`"'
- `ev_006` from `agent2_synthesis` (agent2_traced): [methods] R: 'packageVersion('methylKit')'
- `ev_007` from `agent2_synthesis` (agent2_traced): [discussion] No changelog found: '_No changelog found._'

## Evaluation Strategy
### Direct Checks
- verify file_exists: input CpG text files in package example data directory
- verify script_runs: R code executing unite() on methylRawList object without errors
- verify script_runs: R code executing calculateDiffMeth() on united methylBase object without errors
- verify script_runs: R code executing getMethylDiff() with q-value < 0.01 and 25% difference threshold parameters without errors
- verify output_matches_reference: methylDiff object row count for hyper-methylated bases matches vignette reported value (byte-for-byte exact match required)
- verify output_matches_reference: methylDiff object row count for hypo-methylated bases matches vignette reported value (byte-for-byte exact match required)
- verify field_present: methylDiff output object contains 'hyper' and 'hypo' annotation columns or equivalent slot structure

### Expert Review
- Confirm that the methylDiff object structure and content are consistent with methylKit's documented data model for differential methylation results
- Verify that q-value filtering at threshold 0.01 and 25% methylation difference threshold are correctly applied and produce biologically plausible hyper- vs. hypo-methylated base distributions

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Methodology Summary
1. Load methylation call files using methRead() to obtain methylRawList objects from CpG text files.
2. Merge samples using unite() function to create methylBase object containing only universally covered bases.
3. Calculate differential methylation using calculateDiffMeth(), which applies Fisher's exact test or logistic regression based on sample count.
4. Extract differentially methylated bases using getMethylDiff() with q-value < 0.01 and ≥25% methylation difference thresholds.
5. Validation: Confirm hyper-methylated and hypo-methylated base counts in output methylDiff object match published vignette values.

## Workflow Ports

**Inputs:**

- `cpg_text_files` — methylKit example CpG text files

**Outputs:**

- `methyldiff_object` — methylDiff object with differential methylation results

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:al2na__methylKit`
- **Synthesized at:** 2026-06-15T18:13:27+00:00

## Extraction Quality
- Score: 2/5
- Coherent: false
- Placeholder detected: true
- Groundedness failures (6):
  - research_question: evidence_span not found in section 'results' (value='When differentially methylated bases are extracted from a me', span='After q-value calculation, we can select the differentially ')
  - finding: evidence_span not found in section 'results' (value='Using getMethylDiff() with q-value < 0.01 and 25% methylatio', span='myDiff25p.hyper=getMethylDiff(myDiff,difference=25,qvalue=0.')
  - inputs[0]: evidence_span not found in section 'methods' (value='methylKit example CpG text files (methylation call format fr', span='Starting from the methylKit example CpG text files')
  - expected_outputs[0]: evidence_span not found in section 'methods' (value='methylDiff object containing differentially methylated bases', span='methylDiff object with hyper- and hypo-methylated base count')
  - research_question asks for 'counts' of hyper/hypo-methylated bases, but finding only describes how to extract them as separate objects without reporting actual numeric counts
  - task_objective explicitly requires 'report counts' but finding provides no numeric values or count data
- Notes: This card suffers from a fundamental coherence failure: the research_question explicitly asks 'what are the COUNTS' of hyper vs. hypo-methylated bases, but the finding only describes the R function parameters and object extraction syntax without providing or identifying any actual count numbers. The task_objective says 'report counts' but expected_outputs does not name concrete numeric values or summary statistics. The groundedness failures indicate that evidence_spans are either truncated or mislocated in source sections. Additionally, generic placeholder language ('example data', 'example files') appears throughout rather than article-specific details. The evaluation strategy references vignette-reported count values that should match but these reference benchmarks are never populated in the card itself. For reproducibility and clarity, the card needs: (1) actual numeric count values or ranges for hyper/hypo-methylated bases in expected_outputs, (2) corrected evidence_span citations with full text, (3) specific dataset/file identifiers, (4) vignette reference values explicitly listed.

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
