# SciTask Card: Reproduce the clustering performance comparison: kmers+PCA vs SnapATAC on single-cell ATAC-seq

- Task ID: `task_005`
- Schema version: `0.18.0`
- Created at: `2026-06-15T19:28:53.814125+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_epigenomics/coll_chromvar/synthesized_package`
- Domain: `bioinformatics`
- Subtask categories: `information-extraction`, `data-processing`, `statistical-analysis`
- GitHub: `r3fang/SnapATAC`
- Input from: `task_002`
- Quality: Score 2/5 — Coherent: false, 6 grounding failures

## Classification

- Task kind: `reproduction`
- Article type: `software-tool`
- Primary domain: `genomics`
- Subdomains: `gene-regulation`, `functional-genomics`
- Techniques: `dimensionality-reduction`, `clustering`, `enrichment-analysis`

## Research Question
How does SnapATAC's clustering performance compare to chromVAR variants (especially kmers+PCA) according to the Huidong Chen et al. benchmark study?

## Connected Finding
SnapATAC outperforms chromVAR for clustering tasks evaluated in the Huidong Chen et al. paper, while kmers+PCA is the best chromVAR variant for clustering.

## Task Description
Retrieve the clustering accuracy benchmark table from the Huidong Chen et al. preprint (bioRxiv 739011) comparing chromVAR kmers+PCA, SnapATAC, and other methods across datasets, and produce a structured TSV summary showing method-by-dataset clustering metrics with performance rankings.

## Inputs
- bioRxiv preprint 739011 (Huidong Chen et al.) — clustering benchmark results table or supplementary file

## Expected Outputs
- Structured TSV table with methods as rows, datasets as columns, and clustering accuracy metrics (NMI, ARI, or purity) as cell values
- Summary statistics (mean, median accuracy per method; per-dataset ranking) demonstrating SnapATAC superiority and chromVAR kmers+PCA as best chromVAR variant

## Expected Output File

- `clustering_benchmark_metrics.tsv`

## Landmark Outputs

- `raw_benchmark_table_extracted.txt`
- `clustering_metrics_matrix.tsv`
- `method_summary_statistics.tsv`

## Tools
- SnapATAC
- chromVAR
- R

## Skills
- clustering-accuracy-metric-extraction
- benchmark-table-parsing-and-aggregation
- method-comparison-statistical-summarization
- atac-seq-clustering-performance-interpretation

## Workflow Description
1. Access the bioRxiv preprint 739011 (Chen et al.) and locate the clustering benchmark results table or supplementary data. 2. Extract clustering accuracy metrics (e.g., NMI, ARI, or purity scores) for chromVAR kmers+PCA and SnapATAC variants across all reported datasets. 3. Identify the best-performing chromVAR variant (kmers+PCA) and document relative performance versus SnapATAC baseline. 4. Compile method-by-dataset matrix into a TSV table with rows as methods, columns as datasets, and cells as accuracy scores. 5. Calculate summary statistics (mean, median, rank) for each method across datasets to quantify the finding that SnapATAC outperforms chromVAR overall while chromVAR kmers+PCA is superior to other chromVAR configurations.

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `paper.md` | main_article | True |

## Missing Information
- No changelog found

## Domain Knowledge
- Clustering quality in chromatin accessibility studies is typically measured by normalized mutual information (NMI), adjusted Rand index (ARI), or purity; higher values indicate better separation of known cell/sample types.
- chromVAR kmers+PCA is a specific configuration combining k-mer annotations with principal component analysis dimensionality reduction, distinct from chromVAR with motif-based annotations.
- SnapATAC is a competing single-cell ATAC-seq analysis method that uses a different algorithmic approach (approximate nearest neighbor graphs) and is documented to outperform chromVAR on clustering benchmarks.
- Benchmark tables in bioinformatics preprints typically report metrics across multiple datasets (cell types, organisms, or peak-calling strategies) to demonstrate generalization; missing datasets in the table must be noted.
- The bioRxiv accession 739011 is a publicly deposited preprint version; the benchmark results may differ from a later peer-reviewed publication if one exists.

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.
- Synthesis grounding: the following tools/outputs were NOT found in the source paper and are inferred — verify before use: SnapATAC, Structured TSV table with methods as rows, datasets as columns, and clustering accuracy metrics (NMI, ARI, or purity) as cell values, Summary statistics (mean, median accuracy per method; per-dataset ranking) demonstrating SnapATAC superiority and chromVAR kmers+PCA as best chromVAR variant.

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [intro] How does SnapATAC's clustering performance compare to chromVAR variants (especially kmers+PCA) according to the Huidong Chen et al. benchmark study?: 'For a paper evaluating chromVAR and other methods as a method for enabling clustering of single cells, see [the preprint from Huidong Chen et al](https://www.biorxiv.org/content/10.1101/739011v1).'
- `ev_002` from `agent2_synthesis` (agent2_traced): [intro] SnapATAC outperforms chromVAR for clustering tasks evaluated in the Huidong Chen et al. paper, while kmers+PCA is the best chromVAR variant for clustering.: 'Using kmers + PCA appears to be the best variant of chromVAR for clustering, but newer methods such as SnapATAC outperform chromVAR for the clustering tasks evaluated in the paper.'
- `ev_003` from `agent2_synthesis` (agent2_traced): [other] bioRxiv preprint 739011 (Huidong Chen et al.) — clustering benchmark results table or supplementary file: 'bioRxiv 739011'
- `ev_004` from `agent2_synthesis` (agent2_traced): [other] Structured TSV table with methods as rows, datasets as columns, and clustering accuracy metrics (NMI, ARI, or purity) as cell values: 'structured TSV table of method-by-dataset clustering metrics'
- `ev_005` from `agent2_synthesis` (agent2_traced): [other] Summary statistics (mean, median accuracy per method; per-dataset ranking) demonstrating SnapATAC superiority and chromVAR kmers+PCA as best chromVAR variant: 'summary statistics showing SnapATAC outperforms chromVAR for clustering while chromVAR+kmers+PCA is the best chromVAR variant'
- `ev_006` from `agent2_synthesis` (agent2_traced): [intro] SnapATAC: 'newer methods such as SnapATAC outperform chromVAR for the clustering tasks'
- `ev_007` from `agent2_synthesis` (agent2_traced): [intro] chromVAR: 'chromVAR is an R package for the analysis of sparse chromatin accessibility'
- `ev_008` from `agent2_synthesis` (agent2_traced): [intro] R: 'chromVAR is an R package'
- `ev_009` from `agent2_synthesis` (agent2_traced): [discussion] No changelog found: '_No changelog found._'

## Evaluation Strategy
### Direct Checks
- verify that a TSV table file exists in bioRxiv preprint 739011 (Chen et al.) or its supplementary materials containing clustering accuracy metrics
- file_format_is TSV with columns for method name, dataset identifier, and clustering accuracy metric (e.g., ARI, NMI, or purity score)
- verify that the table row_count_equals at least 3 for chromVAR variants (including kmers+PCA variant) and at least 1 row for SnapATAC, across at least 2 datasets
- value_in_range: clustering accuracy scores are between 0 and 1 (or 0–100 if percentage-scaled); no canonical answer for absolute thresholds since multiple accuracy metrics exist
- verify that SnapATAC row values are numerically greater than or equal to baseline chromVAR rows for the same dataset (robust to metric choice if direction is consistent)
- verify that chromVAR kmers+PCA variant row has the highest or tied-highest accuracy among all chromVAR variants for each dataset, parameter-sensitive to which datasets are included

### Expert Review
- confirm that the retrieved table matches the summary statement 'SnapATAC outperforms chromVAR for clustering' by inspecting whether SnapATAC values exceed chromVAR baseline across all reported datasets
- confirm that the retrieved table supports 'kmers+PCA is the best chromVAR variant' by verifying that kmers+PCA consistently achieves equal or superior clustering accuracy versus other chromVAR approaches (e.g., plain chromVAR, chromVAR+other feature engineering)
- assess whether the table structure and metric definitions are consistent with standard clustering evaluation practice (e.g., higher values indicate better clustering, metric is invariant to label permutation)

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Execution Profile
- **Compute tier:** trivial

## Methodology Summary
1. Locate and download the Huidong Chen et al. bioRxiv 739011 preprint and identify the clustering benchmark results table or supplementary data file.
2. Extract clustering accuracy metrics (NMI, ARI, purity, or equivalent) for chromVAR kmers+PCA, SnapATAC, and all other methods across each reported dataset.
3. Construct a method-by-dataset matrix where rows are methods (chromVAR kmers+PCA, SnapATAC, others), columns are datasets, and cells contain accuracy scores.
4. Calculate summary statistics per method: mean accuracy, median accuracy, and per-dataset rank order to quantify relative performance.
5. Validation: Verify that the table confirms the two key findings: (1) SnapATAC achieves higher mean clustering accuracy than chromVAR variants, and (2) chromVAR kmers+PCA achieves higher accuracy than other chromVAR configurations (motifs, cis-regions, etc.).

## Workflow Ports

**Inputs:**

- `preprint_benchmark_file` — bioRxiv 739011 clustering results ← `task_002/variability_table`

**Outputs:**

- `clustering_metrics_table` — method-by-dataset clustering accuracy TSV
- `summary_statistics` — per-method and per-dataset performance summary

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:GreenleafLab__chromVAR`
- **Synthesized at:** 2026-06-15T19:37:14+00:00

## Extraction Quality
- Score: 2/5
- Coherent: false
- Placeholder detected: false
- Groundedness failures (6):
  - finding: evidence_span not found in section 'intro' (value='SnapATAC outperforms chromVAR for clustering tasks evaluated', span='Using kmers + PCA appears to be the best variant of chromVAR')
  - inputs[0]: evidence_span not found in section 'other' (value='bioRxiv preprint 739011 (Huidong Chen et al.) — clustering b', span='bioRxiv 739011')
  - expected_outputs[0]: evidence_span not found in section 'other' (value='Structured TSV table with methods as rows, datasets as colum', span='structured TSV table of method-by-dataset clustering metrics')
  - expected_outputs[1]: evidence_span not found in section 'other' (value='Summary statistics (mean, median accuracy per method; per-da', span='summary statistics showing SnapATAC outperforms chromVAR for')
  - tools[0]: evidence_span not found in section 'intro' (value='SnapATAC', span='newer methods such as SnapATAC outperform chromVAR for the c')
  - research_question asks specifically about 'clustering performance compare' but evidence_span only cites the existence of the benchmark paper without confirming it contains the specific performance comparison data requested
- Notes: This task card has significant coherence and groundedness issues. The research_question and finding assume the existence and accessibility of quantitative benchmark data (clustering accuracy metrics across methods and datasets) from bioRxiv 739011, but the evidence provided only cites the preprint's existence without confirming the specific benchmark table can be located or extracted. The semantic gap is critical: asking 'how does performance compare' requires actual numerical comparisons, not just reference to a paper that may contain them. The task workflow describes a detailed extraction and aggregation process, but no upstream verification confirms the source data is accessible or matches the described format (method-by-dataset matrix). The finding 'SnapATAC outperforms chromVAR' is somewhat supported by the evidence_span's phrasing ('newer methods such as SnapATAC outperform'), but this is indirect and generic. Recommend: (1) add explicit evidence that bioRxiv 739011 supplementary materials contain a benchmark table with method-by-dataset clustering metrics; (2) provide a snapshot or excerpt confirming the table structure and metric types; (3) clarify whether the paper compares SnapATAC vs. chromVAR directly or whether SnapATAC is mentioned only as a 'newer method' without direct comparison.

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
