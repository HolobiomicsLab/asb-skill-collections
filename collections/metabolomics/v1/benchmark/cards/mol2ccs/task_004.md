# SciTask Card: Extend CCS prediction by substituting an alternative GNN architecture and comparing held-out performance

- Task ID: `task_004`
- Schema version: `0.18.0`
- Created at: `2026-06-15T13:12:43.788519+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_pilot/coll_mol2ccs/synthesized_package`
- Domain: `mass-spectrometry / metabolomics`
- Subtask categories: `model-training`, `benchmark-evaluation`, `data-analysis`
- GitHub: `enveda/ccs-prediction`
- Input from: `task_003`
- Quality: Score 2/5 — Coherent: false, placeholder, 8 grounding failures

## Classification

- Task kind: `extension`
- Article type: `research-article`
- Primary domain: `cheminformatics`
- Subdomains: `artificial-intelligence`, `computational-metabolomics`
- Techniques: `graph-neural-network`, `machine-learning`, `deep-learning`, `transfer-learning`

## Research Question
How do alternative graph neural network architectures (e.g., GAT, MPNN) compare to the original GNN architecture in predicting collision cross section on the same dataset split?

## Connected Finding
The paper evaluates graph neural networks for predicting collision cross section, establishing a baseline for GNN-based approaches that can be extended with alternative architectures.

## Task Description
Replace the original GNN architecture in the enveda/ccs-prediction codebase with an alternative message-passing variant (GAT or MPNN) and re-evaluate on the same CCS dataset split to produce a comparative performance table.

## Inputs
- enveda/ccs-prediction repository containing original GNN model code, preprocessed molecular graph data, and CCS ground-truth labels
- Training, validation, and test dataset splits with molecular structures and corresponding CCS values

## Expected Outputs
- Comparative performance table (CSV or JSON) with metrics (RMSE, MAE, accuracy, training time, inference speed) for both original and alternative GNN architectures
- Trained alternative GNN model weights and architecture definition file
- Predictions on test set from both architectures for downstream comparison

## Expected Output File

- `ccs_gnn_comparison_table.csv`

## Landmark Outputs

- `alt_gnn_architecture.py`
- `training_log_alternative.txt`
- `test_predictions_original.csv`
- `test_predictions_alternative.csv`
- `model_weights_alternative.pt`

## Tools
- PyTorch Geometric (PyG)
- PyTorch

## Skills
- graph-neural-network-architecture-implementation
- message-passing-variant-selection-and-adaptation
- molecular-ccs-prediction-model-training
- regression-metric-computation-and-comparison
- model-hyperparameter-transfer-and-tuning
- inference-performance-benchmarking

## Workflow Description
1. Clone the enveda/ccs-prediction repository and load the original model architecture and preprocessed CCS dataset split (training, validation, test sets). 2. Implement an alternative message-passing GNN architecture (Graph Attention Network or Message-Passing Neural Network) with equivalent input/output dimensions to the original model. 3. Train the alternative GNN on the training set using the same hyperparameters, loss function, and optimization settings as the original baseline, monitoring validation performance. 4. Evaluate both the original and alternative architectures on the held-out test set, computing prediction accuracy, RMSE, MAE, and other relevant regression metrics for CCS prediction. 5. Generate a comparative performance table documenting metric values, training time, and inference speed for both architectures side-by-side.

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `figures/figure_1.png` | figure | False |
| `figures/figure_4.png` | figure | False |
| `figures/figure_5.png` | figure | False |
| `paper.md` | main_article | True |

## Missing Information
- No changelog documenting the specific modifications, alternative architectures tested, or results comparison

## Domain Knowledge
- Message-passing GNNs (GAT, MPNN) operate by iteratively aggregating neighbor features; architectural choice affects receptive field size and learned representations.
- Collision cross section (CCS) is a molecular transport property critical for ion-mobility mass spectrometry; prediction accuracy depends on faithful molecular graph encoding.
- Equivalent input/output dimensions across architectures enable fair comparison; weight initialization and learning rate schedules must be matched to isolate architectural effects.
- Test-set performance (RMSE, MAE) on held-out molecules measures generalization; overfitting risk increases with model capacity without regularization or early stopping.
- Inference speed and memory footprint vary significantly between GAT (attention overhead) and simpler message-passing variants; production deployment trade-offs are non-trivial.

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.
- Synthesis grounding: the following tools/outputs were NOT found in the source paper and are inferred — verify before use: PyTorch Geometric (PyG), PyTorch, Trained alternative GNN model weights and architecture definition file.

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [other] How do alternative graph neural network architectures (e.g., GAT, MPNN) compare to the original GNN architecture in predicting collision cross section on the same dataset split?: 'Evaluating the generalizability of graph neural networks for predicting collision cross section'
- `ev_002` from `agent2_synthesis` (agent2_traced): [other] The paper evaluates graph neural networks for predicting collision cross section, establishing a baseline for GNN-based approaches that can be extended with alternative architectures.: 'Evaluating the generalizability of graph neural networks for predicting collision cross section'
- `ev_003` from `agent2_synthesis` (agent2_traced): [other] enveda/ccs-prediction repository containing original GNN model code, preprocessed molecular graph data, and CCS ground-truth labels: 'enveda/ccs-prediction'
- `ev_004` from `agent2_synthesis` (agent2_traced): [other] Training, validation, and test dataset splits with molecular structures and corresponding CCS values: 'enveda/ccs-prediction'
- `ev_005` from `agent2_synthesis` (agent2_traced): [other] Comparative performance table (CSV or JSON) with metrics (RMSE, MAE, accuracy, training time, inference speed) for both original and alternative GNN architectures: 'enveda/ccs-prediction'
- `ev_006` from `agent2_synthesis` (agent2_traced): [other] Trained alternative GNN model weights and architecture definition file: 'enveda/ccs-prediction'
- `ev_007` from `agent2_synthesis` (agent2_traced): [other] Predictions on test set from both architectures for downstream comparison: 'enveda/ccs-prediction'
- `ev_008` from `agent2_synthesis` (agent2_traced): [other] PyTorch Geometric (PyG): 'enveda/ccs-prediction'
- `ev_009` from `agent2_synthesis` (agent2_traced): [other] PyTorch: 'enveda/ccs-prediction'
- `ev_010` from `agent2_synthesis` (agent2_traced): [discussion] No changelog documenting the specific modifications, alternative architectures tested, or results comparison: 'No changelog found.'

## Evaluation Strategy
### Direct Checks
- verify that the enveda/ccs-prediction repository contains a trained GNN baseline model checkpoint or weights file
- verify that the repository includes a labeled CCS dataset with documented train/validation/test split definitions
- script runs: verify that a Python script implementing an alternative message-passing GNN variant (GAT or MPNN) can be executed without errors on the repository's dataset
- verify that the alternative GNN implementation accepts the same molecular input format as the original GNN architecture
- verify that the re-trained alternative model produces numerical predictions (CCS values or embeddings) on the test split
- verify that a performance comparison table file exists with at least two rows (original GNN + alternative variant) and at least three columns (architecture name, metric1, metric2)
- value_in_range: verify that reported performance metrics (e.g., MAE, RMSE, R²) for both architectures fall within physically plausible bounds for CCS prediction tasks
- verify that the alternative architecture training log or hyperparameter configuration file is reproducibly specified (no canonical answer for optimal hyperparameters; parameter-sensitive)
- output_matches_reference: if the original paper reports baseline performance on this dataset split, verify that the re-implemented baseline GNN reproduces those reported metrics within ±5% relative error (robust to minor implementation variations)

### Expert Review
- confirm that the alternative message-passing variant (GAT or MPNN) represents a meaningful architectural departure from the original GNN (not merely a hyperparameter tune)
- assess whether differences in performance between the original and alternative architecture are scientifically interpretable and likely due to architectural factors rather than training instability
- evaluate whether the experimental setup (same dataset split, comparable training budget) is fair and whether any confounding factors (e.g., different learning rates, batch sizes) are adequately controlled or disclosed
- judge the statistical significance of performance differences if confidence intervals or multiple runs are provided

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Execution Profile
- **Compute tier:** medium

## Methodology Summary
1. Clone enveda/ccs-prediction repository and load preprocessed CCS dataset with train/val/test splits.
2. Implement alternative message-passing GNN (GAT or MPNN) with matching input/output dimensions.
3. Train alternative architecture on training set using original hyperparameters and loss function.
4. Evaluate both original and alternative models on test set, computing RMSE, MAE, accuracy, training time, and inference speed.
5. Generate comparative performance table documenting results side-by-side.
6. Validation: Ensure test-set RMSE and MAE values are computed consistently for both architectures and table is reproducible from stored model weights.

## Workflow Ports

**Inputs:**

- `repo_code` — enveda/ccs-prediction repository with original GNN implementation ← `task_003/graph_dataset`
- `ccs_dataset` — Preprocessed CCS dataset with train/val/test splits and molecular graphs

**Outputs:**

- `performance_table` — Comparative metrics table for original vs. alternative GNN
- `alt_model_weights` — Trained alternative GNN model weights
- `test_predictions` — Predictions from both models on test set

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:enveda__ccs-prediction`
- **Synthesized at:** 2026-06-15T13:16:33+00:00

## Extraction Quality
- Score: 2/5
- Coherent: false
- Placeholder detected: true
- Groundedness failures (8):
  - inputs[0]: evidence_span not found in section 'other' (value='enveda/ccs-prediction repository containing original GNN mod', span='enveda/ccs-prediction')
  - inputs[1]: evidence_span not found in section 'other' (value='Training, validation, and test dataset splits with molecular', span='enveda/ccs-prediction')
  - expected_outputs[0]: evidence_span not found in section 'other' (value='Comparative performance table (CSV or JSON) with metrics (RM', span='enveda/ccs-prediction')
  - expected_outputs[1]: evidence_span not found in section 'other' (value='Trained alternative GNN model weights and architecture defin', span='enveda/ccs-prediction')
  - expected_outputs[2]: evidence_span not found in section 'other' (value='Predictions on test set from both architectures for downstre', span='enveda/ccs-prediction')
  - tools[0]: evidence_span not found in section 'other' (value='PyTorch Geometric (PyG)', span='enveda/ccs-prediction')
  - tools[1]: evidence_span not found in section 'other' (value='PyTorch', span='enveda/ccs-prediction')
  - SEMANTIC GAP: research_question asks 'how do alternative architectures compare' but grounding evidence only states the paper 'evaluates graph neural networks for predicting collision cross section'—no mention of comparing alternatives or establishing that baseline extension is documented in the source
- Notes: This task card has low quality due to fundamental misalignment between its framing and grounding. The research_question implies the source paper compares alternative GNN architectures, but the finding only claims the paper establishes a baseline for future extension. No evidence_span actually quotes or grounds the claim that such comparisons exist in the source. All inputs/outputs/tools reference 'enveda/ccs-prediction' (a repository identifier) rather than article text, failing substring-match grounding. The task itself is well-specified (implement GAT/MPNN, compare on test set) but should be reframed as an extension or replication task, not as evaluating claimed research findings. Recommend: (1) Clarify whether the source paper discusses alternative GNN variants; (2) If not, reclassify as a pure implementation task with no grounding requirement; (3) If yes, provide actual evidence_span quotations; (4) Ensure research_question matches the finding or vice versa. Current quality too low for structured evaluation.

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
