# SciTask Card: Reconstruct the buildExperiment SummarizedExperiment construction step from a tab-delimited input file

- Task ID: `task_001`
- Schema version: `0.18.0`
- Created at: `2026-06-15T13:20:51.739319+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_pilot/coll_mzquality/synthesized_package`
- Domain: `mass-spectrometry / metabolomics`
- Subtask categories: `data-processing`, `information-extraction`
- GitHub: `hankemeierlab/mzQuality`
- Quality: Score 2/5 — Coherent: false, placeholder, 5 grounding failures

## Classification

- Task kind: `component_reconstruction`
- Article type: `software-tool`
- Primary domain: `metabolomics`
- Subdomains: `untargeted-metabolomics`, `computational-metabolomics`
- Techniques: `quality-control`, `feature-detection`, `lc-ms`, `normalization`, `statistical-analysis`

## Research Question
Does the buildExperiment function correctly construct a SummarizedExperiment object with a computed ratio assay when given a data frame with specified column mappings for compounds, samples, and internal standards?

## Connected Finding
The buildExperiment function constructs a SummarizedExperiment object from a data frame by mapping user-specified columns to compound names, sample names, primary assay (compound area), secondary assay (internal standard area), and sample types. The function automatically calculates the compound/internal standard ratio for each sample and stores it in the 'ratio' assay, with the ratio computed as primary assay divided by secondary assay (or set to primary assay if secondary assay is not provided).

## Task Description
Load the example.tsv file using readData, then construct a SummarizedExperiment object via buildExperiment with documented column mappings (aliquot, compound, area, type, injection_time, batch, concentration) to produce an experiment object containing the 'ratio' assay. Verify object structure including rowData, colData, and assays.

## Inputs
- example.tsv file from mzQuality package (extdata/example.tsv)

## Expected Outputs
- SummarizedExperiment object with rowData, colData, and assays including 'ratio'

## Landmark Outputs

- `cleaned_data_frame.tsv`
- `se_object.rds`

## Tools
- mzQuality
- R
- SummarizedExperiment

## Skills
- metabolomics-data-input-validation
- summarized-experiment-object-construction
- chromatographic-assay-ratio-computation
- bioconductor-object-structure-inspection
- tab-delimited-metabolomics-file-parsing

## Workflow Description
1. Load the example.tsv file from the mzQuality package using readData, which performs basic column validation and data integrity checks. 2. Call buildExperiment on the resulting data frame, specifying column mappings for aliquots (samples), compounds (features), and assay values (area under the curve). 3. Inspect the returned SummarizedExperiment object to confirm presence of rowData (compound metadata), colData (sample metadata), and assays (including the computed 'ratio' assay derived from compound and internal standard areas).

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `figures/Rplot001.jpg` | figure | False |
| `paper.md` | main_article | True |

## Missing Information
- No changelog found.

## Domain Knowledge
- The mzQuality input format requires six mandatory columns (aliquot, compound, area, type, injection_time, batch) plus optional concentration column for calibration samples.
- The 'ratio' assay is computed as the compound area normalized by internal standard (area_is) to account for injection variability in mass spectrometry measurements.
- A SummarizedExperiment object stores features as rows, samples as columns, and numeric matrices as assays, with associated rowData and colData metadata frames.
- Missing concentrations in study samples (non-calibration rows) must be explicitly marked as NA, not omitted, to preserve sample integrity in the experiment object.
- The readData function performs column presence and data-type validation before passing cleaned data to buildExperiment for object construction.

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.
- Synthesis grounding: the following tools/outputs were NOT found in the source paper and are inferred — verify before use: SummarizedExperiment, SummarizedExperiment object with rowData, colData, and assays including 'ratio'.

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [results] Does the buildExperiment function correctly construct a SummarizedExperiment object with a computed ratio assay when given a data frame with specified column mappings for compounds, samples, and internal standards?: 'The `buildExperiment` function allows you to create a *SummarizedExperiment* object from a data frame by specifying the following columns: `compoundColumn`, `aliquotColumn`, `primaryAssay`,'
- `ev_002` from `agent2_synthesis` (agent2_traced): [results] The buildExperiment function constructs a SummarizedExperiment object from a data frame by mapping user-specified columns to compound names, sample names, primary assay (compound area), secondary assay (internal standard area), and sample types. The function automatically calculates the compound/internal standard ratio for each sample and stores it in the 'ratio' assay, with the ratio computed as primary assay divided by secondary assay (or set to primary assay if secondary assay is not provided).: 'The `buildExperiment` function will automatically calculate the compound / Internal Standard ratio for each sample and store it in the `ratio` assay. Note that if the `secondaryAssay` column is not'
- `ev_003` from `agent2_synthesis` (agent2_traced): [other] example.tsv file from mzQuality package (extdata/example.tsv): 'file <- system.file("extdata/example.tsv", package = "mzQuality")'
- `ev_004` from `agent2_synthesis` (agent2_traced): [other] SummarizedExperiment object with rowData, colData, and assays including 'ratio': 'The `buildExperiment` function will then take the data and create an experiment object that can be used for analysis.'
- `ev_005` from `agent2_synthesis` (agent2_traced): [other] mzQuality: 'library(mzQuality)'
- `ev_006` from `agent2_synthesis` (agent2_traced): [other] R: 'library(mzQuality)'
- `ev_007` from `agent2_synthesis` (agent2_traced): [other] SummarizedExperiment: 'The `buildExperiment` function will then take the data and create an experiment object that can be used for analysis.'
- `ev_008` from `agent2_synthesis` (agent2_traced): [discussion] No changelog found.: '_No changelog found._'

## Evaluation Strategy
### Direct Checks
- verify file example.tsv exists in package or accessible repository
- verify readData function runs without error on example.tsv as input
- verify buildExperiment function runs without error given readData output and documented column mappings
- verify output object is of class SummarizedExperiment
- verify output object contains 'ratio' assay (verify assays(output) contains key 'ratio')
- verify output object contains non-empty rowData slot
- verify output object contains non-empty colData slot
- verify output object structure has three expected slots: rowData, colData, assays (robust to parameter choices)

### Expert Review
- verify that the 'ratio' assay contains biologically plausible values for metabolomics ratio data
- verify that rowData annotations (compound identifiers, metadata) align with expected metabolomics quality control metadata
- verify that colData annotations (sample identifiers, types, batch information) align with documented column mappings

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Execution Profile
- **Compute tier:** trivial

## Methodology Summary
1. Load example.tsv from mzQuality package using readData function with built-in column validation.
2. Map required columns (aliquot, compound, area, type, injection_time, batch) and optional concentration column to data frame.
3. Call buildExperiment to construct a SummarizedExperiment object with features (compounds) as rows and samples (aliquots) as columns.
4. Verify SummarizedExperiment structure: rowData contains compound identifiers, colData contains sample metadata, and assays include the computed ratio (area / area_is).
5. Validation: SummarizedExperiment object can be created without errors, contains non-empty rowData and colData frames, and ratio assay is present and numeric.

## Workflow Ports

**Inputs:**

- `example_tsv` — example.tsv file from mzQuality package

**Outputs:**

- `experiment_object` — SummarizedExperiment object with assays and metadata

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:hankemeierlab__mzQuality`
- **Synthesized at:** 2026-06-15T13:26:39+00:00

## Extraction Quality
- Score: 2/5
- Coherent: false
- Placeholder detected: true
- Groundedness failures (5):
  - research_question: evidence_span not found in section 'results' (value='Does the buildExperiment function correctly construct a Summ', span='The `buildExperiment` function allows you to create a *Summa')
  - finding: evidence_span not found in section 'results' (value='The buildExperiment function constructs a SummarizedExperime', span='The `buildExperiment` function will automatically calculate ')
  - research_question evidence_span is incomplete/truncated and does not fully cover the claimed functionality (does not substantiate 'computed ratio assay' or 'internal standards' claims)
  - finding evidence_span is incomplete and does not substantiate the claim about ratio calculation formula (primary/secondary) or NA handling when secondaryAssay is absent
  - expected_outputs evidence_span ('The `buildExperiment` function will then take the data and create an experiment object that can be used for analysis.') is generic boilerplate and does not ground the specific claim about rowData, colData, and 'ratio' assay
- Notes: This card has significant groundedness and coherence issues. The research_question and finding are not properly grounded in the provided evidence spans—both key claims about ratio computation are either absent from or truncated in their cited evidence. The finding makes specific claims about computation logic (primary/secondary division, NA fallback) that are not substantiated by the incomplete evidence_span. Additionally, there is semantic mismatch between the technical claims in the research_question/finding and the generic boilerplate in the grounding evidence. The task is well-structured methodologically, but the scientific claims need stronger, complete grounding with explicit examples or code demonstrations of the ratio assay computation.

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
