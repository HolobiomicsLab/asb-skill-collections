# SciTask Card: Extend mzQuality to support a no-internal-standard condition and verify ratio assay defaults to 1

- Task ID: `task_005`
- Schema version: `0.18.0`
- Created at: `2026-06-15T13:20:51.739319+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_pilot/coll_mzquality/synthesized_package`
- Domain: `mass-spectrometry / metabolomics`
- Subtask categories: `data-processing`, `benchmark-evaluation`
- GitHub: `hankemeierlab/mzQuality`
- Input from: `task_001`
- Quality: Score 2/5 — Coherent: false, placeholder, 3 grounding failures

## Classification

- Task kind: `extension`
- Article type: `software-tool`
- Primary domain: `metabolomics`
- Subdomains: `untargeted-metabolomics`, `computational-metabolomics`
- Techniques: `quality-control`, `feature-detection`, `lc-ms`, `normalization`, `statistical-analysis`

## Research Question
When the secondaryAssay column is not provided to buildExperiment, does the resulting 'ratio' assay equal the primary assay values unchanged?

## Connected Finding
When secondaryAssay is not provided to buildExperiment, its value defaults to 1, which negates the Internal Standard effect, making the ratio assay equal to the primary assay values.

## Task Description
Load a tab-delimited metabolomics data file (example.tsv) using readData, construct a SummarizedExperiment object via buildExperiment without the secondaryAssay column, and verify that the resulting 'ratio' assay entries equal the raw primary assay values (divisor = 1 throughout).

## Inputs
- example.tsv — tab-delimited metabolomics data file containing columns: aliquot, compound, area, compound_is, area_is, type, injection_time, batch, and concentration

## Expected Outputs
- Validation report (CSV or text) confirming that all 'ratio' assay values equal corresponding primary assay values within numerical tolerance (machine epsilon)

## Expected Output File

- `ratio_assay_validation.csv`

## Landmark Outputs

- `cleaned_data_frame.csv`
- `experiment_object_summary.txt`

## Tools
- R
- mzQuality
- SummarizedExperiment

## Skills
- metabolomics-data-format-validation
- r-package-api-usage
- summarized-experiment-object-construction
- assay-ratio-computation-verification
- numerical-equivalence-testing

## Workflow Description
1. Load the example.tsv file from the mzQuality package using readData function, which performs basic checks on column integrity. 2. Call buildExperiment on the resulting data frame without specifying a secondaryAssay column to generate a SummarizedExperiment object. 3. Extract the 'ratio' assay from the experiment object and compare element-wise to the primary assay values to verify identity (effective divisor of 1). 4. Generate a validation report confirming equality across all compound–sample pairs.

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `figures/Rplot001.jpg` | figure | False |
| `paper.md` | main_article | True |

## Missing Information
- No changelog found

## Domain Knowledge
- mzQuality's buildExperiment function auto-generates a 'ratio' assay by dividing the primary assay by a secondary assay (internal standard area); when no secondaryAssay column is supplied, the function must handle this gracefully, typically by setting divisor to 1 or omitting the ratio calculation.
- The SummarizedExperiment object stores assays as a list of matrices, each indexed by assay name; verification requires extraction and element-wise comparison.
- Tab-delimited format requires exact column names (aliquot, compound, area, type, injection_time, batch) as recognized by readData; missing or misspelled columns will trigger validation failure.
- Numerical comparison of floating-point assay values must account for rounding error; exact equality is not expected, but relative difference should be within machine epsilon (~1e-15 for double precision).

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.
- Synthesis grounding: the following tools/outputs were NOT found in the source paper and are inferred — verify before use: SummarizedExperiment, Validation report (CSV or text) confirming that all 'ratio' assay values equal corresponding primary assay values within numerical tolerance (machine epsilon).

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [other] When the secondaryAssay column is not provided to buildExperiment, does the resulting 'ratio' assay equal the primary assay values unchanged?: 'Note that if the `secondaryAssay` column is not provided, its value will be set to `1`, effectively negating the Internal Standard.'
- `ev_002` from `agent2_synthesis` (agent2_traced): [other] When secondaryAssay is not provided to buildExperiment, its value defaults to 1, which negates the Internal Standard effect, making the ratio assay equal to the primary assay values.: 'Note that if the `secondaryAssay` column is not provided, its value will be set to `1`, effectively negating the Internal Standard.'
- `ev_003` from `agent2_synthesis` (agent2_traced): [other] example.tsv — tab-delimited metabolomics data file containing columns: aliquot, compound, area, compound_is, area_is, type, injection_time, batch, and concentration: 'The data should be in a tab-delimited format containing at least the following columns: aliquot, compound, area, type, injection_time, batch.'
- `ev_004` from `agent2_synthesis` (agent2_traced): [other] Validation report (CSV or text) confirming that all 'ratio' assay values equal corresponding primary assay values within numerical tolerance (machine epsilon): 'The `buildExperiment` function will then take the data and create an experiment object that can be used for analysis.'
- `ev_005` from `agent2_synthesis` (agent2_traced): [other] R: 'library(mzQuality)'
- `ev_006` from `agent2_synthesis` (agent2_traced): [other] mzQuality: 'mzQuality requires a specific format for the input data.'
- `ev_007` from `agent2_synthesis` (agent2_traced): [intro] SummarizedExperiment: 'Internally, mzQuality uses Bioconductors' *SummarizedExperiment* object to store the data.'
- `ev_008` from `agent2_synthesis` (agent2_traced): [discussion] No changelog found: '_No changelog found._'

## Evaluation Strategy
### Direct Checks
- verify file 'example.tsv' exists in the mzQuality package repository or deposited dataset
- script_runs: execute buildExperiment(example.tsv, primaryAssay='<identified column>', secondaryAssay=NULL) without error in R environment with mzQuality loaded
- field_present: verify 'ratio' assay is present in resulting SummarizedExperiment object
- output_matches_reference: extract all 'ratio' assay values from output and verify they equal corresponding primary assay values (robust to floating-point precision within machine epsilon)

### Expert Review
- verify that the conditional behaviour documented in the article (divisor defaults to 1 when secondaryAssay is omitted) is correctly implemented and produces mathematically sound ratio calculations
- confirm that the ratio assay calculation logic aligns with the stated package design for handling missing secondary assay specifications

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Execution Profile
- **Compute tier:** trivial

## Methodology Summary
1. Load example.tsv using readData function to validate column structure and data integrity.
2. Construct SummarizedExperiment object via buildExperiment without secondaryAssay parameter.
3. Extract 'ratio' and primary assays from the experiment object.
4. Perform element-wise numerical comparison of ratio assay to primary assay values.
5. Validation: all ratio values must equal primary assay values within double-precision floating-point tolerance (relative difference < 1e-14).

## Workflow Ports

**Inputs:**

- `example_tsv` — example.tsv metabolomics data file ← `task_001/experiment_object`

**Outputs:**

- `validation_report` — ratio-assay equality validation report

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:hankemeierlab__mzQuality`
- **Synthesized at:** 2026-06-15T13:27:50+00:00

## Extraction Quality
- Score: 2/5
- Coherent: false
- Placeholder detected: true
- Groundedness failures (3):
  - inputs[0]: evidence_span not found in section 'other' (value='example.tsv — tab-delimited metabolomics data file containin', span='The data should be in a tab-delimited format containing at l')
  - expected_outputs[0]: evidence_span is generic and does not specifically support the claim about 'ratio' assay equality validation; span talks about buildExperiment creating 'an experiment object' without mentioning ratio assay or equality checking
  - research_question vs finding: semantic gap — research_question asks 'does ratio equal primary values unchanged?' but finding only affirms that divisor becomes 1 (the mechanism), not that this produces equality with primary assay values; the logical inference is unstated in the evidence
- Notes: The task card conflates the documentation claim (divisor defaults to 1) with an untested hypothesis (ratio then equals primary values). The research_question and finding are semantically related but the finding does not fully answer the question without assuming the ratio calculation formula is primary/divisor. Multiple fields use generic language ('a tool', 'validation report', 'the data') instead of article-specific details. Groundedness is weak: input evidence_span does not match source text, output evidence is off-topic, and the critical mathematical property (ratio = primary / 1 = primary) is inferred rather than grounded. Recommend: (1) add explicit evidence for the ratio calculation formula, (2) reground inputs and outputs against actual documentation, (3) clarify expected output structure and numerical tolerance justification, (4) reconcile landmark_outputs with methodology.

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
