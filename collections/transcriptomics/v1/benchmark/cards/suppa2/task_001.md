# SciTask Card: Reconstruct the generateEvents command to produce ioe, ioi, and gtf artifacts from a GTF input

- Task ID: `task_001`
- Schema version: `0.18.0`
- Created at: `2026-06-15T17:50:44.296688+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_transcriptomics/coll_suppa2/synthesized_package`
- Domain: `bioinformatics`
- Subtask categories: `data-processing`, `information-extraction`
- GitHub: `comprna/SUPPA`
- Quality: Score 2/5 — Coherent: false, placeholder, 6 grounding failures

## Classification

- Task kind: `component_reconstruction`
- Article type: `software-tool`
- Primary domain: `transcriptomics`
- Subdomains: `differential-expression`, `gene-regulation`
- Techniques: `statistical-analysis`, `differential-abundance-analysis`

## Research Question
What are the three distinct output file artifacts produced by the SUPPA2 generateEvents subcommand when parsing a genome annotation GTF file?

## Connected Finding
The SUPPA2 generateEvents subcommand produces three named output artifacts: ioi (input-output index), ioe (input-output events), and gtf for local events, which are generated from parsing a genome annotation GTF file.

## Task Description
Implement the SUPPA2 generateEvents subcommand to parse a genome annotation GTF file and produce three named output artifacts: an ioe file (transcript events), an ioi file (event indices), and a gtf file containing local alternative splicing events.

## Inputs
- Genome annotation GTF file

## Expected Outputs
- ioe file (index of events) mapping transcripts to events
- ioi file (index of isoforms) mapping events to isoforms
- gtf file containing local alternative splicing events

## Landmark Outputs

- `*.ioe`
- `*.ioi`
- `*_local_events.gtf`

## Tools
- SUPPA2

## Skills
- gtf-file-parsing
- transcript-event-extraction
- alternative-splicing-event-identification
- isoform-structure-annotation
- local-splicing-event-detection

## Workflow Description
1. Load genome annotation GTF file into SUPPA2 generateEvents subcommand. 2. Parse GTF to extract transcript structures and identify all transcript-level events (exon inclusion, alternative promoters, alternative terminators). 3. Generate ioe (index of events) output file mapping transcript identifiers to event definitions. 4. Generate ioi (index of isoforms) output file mapping events to isoforms. 5. Identify and extract local alternative splicing events (exon skipping, intron retention, alternative splice sites) from the annotation. 6. Generate gtf output file containing only local alternative splicing event coordinates.

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `paper.md` | main_article | True |

## Missing Information
- No changelog or version history information is provided

## Domain Knowledge
- GTF format encodes genomic coordinates, feature types (exon, start_codon, stop_codon), and transcript-to-gene mappings required to build isoform structures.
- Transcript-level events span all exonic boundaries and include promoter/terminator usage, while local events represent pairwise exon relationships (e.g. exon skipping, intron retention, alternative 5'/3' sites).
- The ioe file must contain all possible splicing events derivable from the annotation; the ioi file must link each event to the set of isoforms expressing or excluding it.
- Local alternative splicing events are extracted only from exon-to-exon junctions that exhibit variation across isoforms in the input annotation.

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.
- Synthesis grounding: the following tools/outputs were NOT found in the source paper and are inferred — verify before use: ioe file (index of events) mapping transcripts to events, ioi file (index of isoforms) mapping events to isoforms, gtf file containing local alternative splicing events.

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [other] What are the three distinct output file artifacts produced by the SUPPA2 generateEvents subcommand when parsing a genome annotation GTF file?: 'Output files
         * [ioi](#ioi)
         * [ioe](#ioe)
         * [gtf](#gtf-for-local-events)'
- `ev_002` from `agent2_synthesis` (agent2_traced): [other] The SUPPA2 generateEvents subcommand produces three named output artifacts: ioi (input-output index), ioe (input-output events), and gtf for local events, which are generated from parsing a genome annotation GTF file.: 'Output files
         * [ioi](#ioi)
         * [ioe](#ioe)
         * [gtf](#gtf-for-local-events)'
- `ev_003` from `agent2_synthesis` (agent2_traced): [methods] Genome annotation GTF file: 'parse a genome annotation GTF file'
- `ev_004` from `agent2_synthesis` (agent2_traced): [methods] ioe file (index of events) mapping transcripts to events: 'ioe file'
- `ev_005` from `agent2_synthesis` (agent2_traced): [methods] ioi file (index of isoforms) mapping events to isoforms: 'ioi file'
- `ev_006` from `agent2_synthesis` (agent2_traced): [methods] gtf file containing local alternative splicing events: 'gtf for local events'
- `ev_007` from `agent2_synthesis` (agent2_traced): [intro] SUPPA2: 'SUPPA2: fast, accurate, and uncertainty-aware differential splicing analysis across multiple conditions'
- `ev_008` from `agent2_synthesis` (agent2_traced): [discussion] No changelog or version history information is provided: '_No changelog found._'

## Evaluation Strategy
### Direct Checks
- verify that github:comprna__SUPPA repository is accessible and contains SUPPA2 source code
- verify file_exists: GTF input file is loadable and conforms to GTF format specification
- verify file_exists: output ioe file (isoform events) is generated with expected structure
- verify file_exists: output ioi file (isoform-isoform events) is generated with expected structure
- verify file_exists: output GTF file for local events is generated in valid GTF format
- verify row_count_equals or value_in_range: output files contain non-zero number of event records (parameter-sensitive to input annotation complexity)
- verify format_is: ioe file format matches SUPPA2 specification for isoform event output
- verify format_is: ioi file format matches SUPPA2 specification for isoform-isoform event output
- verify script_runs: SUPPA2 generateEvents subcommand executes without fatal errors on provided GTF input
- verify contains_substring: output files contain expected event type identifiers (e.g., exon skipping, alternative 5'/3' splice sites)

### Expert Review
- inspect biological plausibility of generated events: verify that isoform events correspond to known alternative splicing mechanisms and are consistent with input GTF transcript structures
- inspect consistency between three outputs: verify that events in ioe file, ioi file, and local-events GTF file are mutually coherent and do not contain logical contradictions
- inspect accuracy of event definition: verify that exon coordinates, splice sites, and event boundaries in output files correctly reflect the input GTF annotation

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Methodology Summary
1. Load genome annotation in GTF format into SUPPA2 generateEvents subcommand.
2. Extract transcript structures and identify all exonic features, promoter/terminator variants, and isoform-specific junctions.
3. Generate index of events (ioe) file enumerating transcript-level and local splicing events with event definitions.
4. Generate index of isoforms (ioi) file mapping each event to the set of isoforms that include or exclude it.
5. Filter and extract only local alternative splicing events (exon skipping, intron retention, alternative splice sites) into a separate GTF file.
6. Validation: verify that all three output files are produced with correct format and non-empty event/isoform mappings consistent with input annotation complexity.

## Workflow Ports

**Inputs:**

- `annotation_gtf` — Genome annotation GTF file

**Outputs:**

- `ioe_file` — Index of events file
- `ioi_file` — Index of isoforms file
- `local_events_gtf` — GTF file with local alternative splicing events

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:comprna__SUPPA`
- **Synthesized at:** 2026-06-15T17:53:39+00:00

## Extraction Quality
- Score: 2/5
- Coherent: false
- Placeholder detected: true
- Groundedness failures (6):
  - inputs[0]: evidence_span not found in section 'methods' (value='Genome annotation GTF file', span='parse a genome annotation GTF file')
  - expected_outputs[0]: evidence_span not found in section 'methods' (value='ioe file (index of events) mapping transcripts to events', span='ioe file')
  - expected_outputs[1]: evidence_span not found in section 'methods' (value='ioi file (index of isoforms) mapping events to isoforms', span='ioi file')
  - expected_outputs[2]: evidence_span not found in section 'methods' (value='gtf file containing local alternative splicing events', span='gtf for local events')
  - SEMANTIC GAP: research_question and finding both claim specific output file meanings (ioi='input-output index', ioe='input-output events') but the evidence_span only lists bare filenames with anchor links; no definitions are present in source text to support the acronym expansions
  - SEMANTIC GAP: task_description elaborates substantially on ioe and ioi meanings ('transcript events', 'event indices', 'isoform mapping') beyond what is grounded in the evidence_span
- Notes: This card exhibits significant coherence problems. While the research_question and finding are semantically aligned (both ask about three output artifacts from generateEvents), the grounding is weak: the evidence_span is section-mismatched and provides only a bare bulleted list of filenames without definitions, structure, or functional descriptions. The card then extensively elaborates these artifacts in task_description, workflow_description, and domain_knowledge with specific biological and technical meanings (e.g., ioe maps transcripts to events, ioi maps events to isoforms) that are not present in the evidence text. This creates a false sense of grounding. The acronym expansions in the finding ('ioi = input-output index', 'ioe = input-output events') appear invented and are not evidenced. Additionally, section assignments are inconsistent (evidence cited as 'methods' but actually from 'other'). The card would need either (1) quoted evidence that explicitly defines what each output file contains and how it is structured, or (2) removal of specific claims about file contents and reversion to generic descriptions matching the evidence. Quality score reflects multiple critical groundedness and coherence failures.

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
