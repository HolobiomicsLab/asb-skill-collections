# SciTask Card: Reproduce tx2gene table construction from Gencode v27 and tximport gene-level count matrix

- Task ID: `task_001`
- Schema version: `0.18.0`
- Created at: `2026-06-15T19:03:30.531922+00:00`
- Source package: `/Users/nothiasl/git/AgenticScienceBuilder/outputs/asbb_transcriptomics/coll_tximport/synthesized_package`
- Domain: `bioinformatics`
- Subtask categories: `data-processing`, `information-extraction`
- DOI: `10.12688/f1000research.7563.1`
- GitHub: `thelovelab/tximport`

## Classification

- Task kind: `reproduction`
- Article type: `research-article`
- Primary domain: `transcriptomics`
- Subdomains: `differential-expression`, `rna-seq`
- Techniques: `normalization`, `statistical-analysis`

## Research Question
Can tximport successfully import salmon transcript-level quantification files and produce gene-level counts, abundance, and length matrices using a pre-constructed tx2gene mapping?

## Connected Finding
tximport with type='salmon' and a tx2gene mapping produces a list containing gene-level abundance, counts, and length matrices from salmon quant.sf.gz files.

## Task Description
Import transcript-level abundance estimates from six salmon quant.sf.gz samples using tximport with the pre-built tx2gene.gencode.v27.csv mapping, and generate gene-level counts, abundance, and length matrices.

## Inputs
- Six salmon quant.sf.gz quantification files from tximportData package
- tx2gene.gencode.v27.csv file mapping transcript IDs to gene IDs

## Expected Outputs
- Gene-level counts matrix (rows: genes, columns: samples)
- Gene-level abundance matrix in TPM (rows: genes, columns: samples)
- Gene-level length matrix of effective transcript lengths (rows: genes, columns: samples)

## Landmark Outputs

- `tx2gene_loaded.csv`
- `salmon_files_list.txt`
- `tximport_output_list.rds`
- `gene_counts.csv`
- `gene_abundance.csv`
- `gene_length.csv`

## Tools
- tximport
- readr

## Skills
- transcript-to-gene-mapping
- transcript-abundance-quantification-import
- gene-level-summarization-from-transcripts
- salmon-output-parsing
- rna-seq-count-matrix-generation

## Workflow Description
1. Load the tx2gene mapping file (tx2gene.gencode.v27.csv) containing transcript-to-gene associations. 2. Read the six salmon quantification files (quant.sf.gz) using tximport with type='salmon' argument. 3. Summarize transcript-level estimates to gene-level by mapping transcripts to genes via tx2gene. 4. Generate and export three matrices: gene-level counts, abundance (TPM), and effective transcript length.

## Available Artifacts
| Path | Role | Indexable |
|---|---|---|
| `paper.md` | main_article | True |

## Missing Information
- No changelog or version history documented for the tximportData package

## Domain Knowledge
- tximport corrects for potential changes in gene length across samples from differential isoform usage, enabling more accurate gene-level inference.
- The tx2gene mapping file is required to associate transcript-level quantification estimates with gene identifiers for aggregation.
- The type argument in tximport must be set to 'salmon' to correctly parse salmon quant.sf.gz output format.
- The returned list contains three matrices: abundance (estimated TPM), counts (estimated number of reads assigned), and length (effective transcript length per gene per sample).

## Uncertainty Notes
- This card was generated by the LLM-assisted pipeline and needs scientific expert review.
- Each TracedClaim's evidence_span has been substring-checked against its source section; see logs/llm_calls.jsonl and capsules/<task_id>/quality_report.json for groundedness results.
- Synthesis grounding: the following tools/outputs were NOT found in the source paper and are inferred — verify before use: readr.

## Evidence Snippets
- `ev_001` from `agent2_synthesis` (agent2_traced): [results] Can tximport successfully import salmon transcript-level quantification files and produce gene-level counts, abundance, and length matrices using a pre-constructed tx2gene mapping?: 'The tximport package has a single function for importing transcript-level estimates. The `type` argument is used to specify what software was used for estimation. A simple list with matrices,'
- `ev_002` from `agent2_synthesis` (agent2_traced): [results] tximport with type='salmon' and a tx2gene mapping produces a list containing gene-level abundance, counts, and length matrices from salmon quant.sf.gz files.: 'library(tximport)
txi <- tximport(files, type="salmon", tx2gene=tx2gene)
names(txi)
head(txi$counts)'
- `ev_003` from `agent2_synthesis` (agent2_traced): [methods] Six salmon quant.sf.gz quantification files from tximportData package: 'Importing transcript abundance with tximport'
- `ev_004` from `agent2_synthesis` (agent2_traced): [methods] tx2gene.gencode.v27.csv file mapping transcript IDs to gene IDs: 'pre-built tx2gene.gencode.v27.csv'
- `ev_005` from `agent2_synthesis` (agent2_traced): [intro] Gene-level counts matrix (rows: genes, columns: samples): 'A simple list with matrices, 'abundance', 'counts', and 'length', is returned, where the transcript level information is summarized to the gene-level'
- `ev_006` from `agent2_synthesis` (agent2_traced): [intro] Gene-level abundance matrix in TPM (rows: genes, columns: samples): 'A simple list with matrices, 'abundance', 'counts', and 'length', is returned, where the transcript level information is summarized to the gene-level'
- `ev_007` from `agent2_synthesis` (agent2_traced): [intro] Gene-level length matrix of effective transcript lengths (rows: genes, columns: samples): 'A simple list with matrices, 'abundance', 'counts', and 'length', is returned, where the transcript level information is summarized to the gene-level'
- `ev_008` from `agent2_synthesis` (agent2_traced): [intro] tximport: 'Imports transcript-level abundance, estimated counts and transcript lengths, and summarizes into matrices for use with downstream statistical analysis packages'
- `ev_009` from `agent2_synthesis` (agent2_traced): [intro] readr: 'While tximport works without any dependencies, it is significantly faster to read in files using the readr package'
- `ev_010` from `agent2_synthesis` (agent2_traced): [discussion] No changelog or version history documented for the tximportData package: '_No changelog found._'

## Evaluation Strategy
### Direct Checks
- verify file 'tx2gene.gencode.v27.csv' exists in tximportData package
- verify six salmon quant.sf.gz sample files exist in tximportData package
- script_runs: tximport function executes without error on the six salmon samples with tx2gene mapping
- output_matches_reference: counts matrix dimensions are consistent with gene count (rows) × sample count (columns)
- output_matches_reference: abundance matrix dimensions are consistent with gene count (rows) × sample count (columns)
- output_matches_reference: length matrix dimensions are consistent with gene count (rows) × sample count (columns)
- verify counts matrix contains non-negative numeric values
- verify abundance matrix contains non-negative numeric values
- verify length matrix contains positive numeric values

### Expert Review
- verify that the three returned matrices (counts, abundance, length) are biologically plausible for RNA-seq transcript aggregation, robust to parameter choices
- verify that length-corrected counts appropriately account for differential isoform usage across samples

## Review Questions
- Is the research question correctly identified and scoped?
- Does the connected finding have enough supporting evidence?
- Which artifacts are required before this can become an executable benchmark task?
- What direct, visual, textual, or expert-review checks should be used for evaluation?

## Methodology Summary
1. Load the tx2gene.gencode.v27.csv file containing transcript-to-gene ID mappings.
2. Read six salmon quant.sf.gz files and parse quantification estimates using tximport with type='salmon'.
3. Aggregate transcript-level estimates to gene-level by summing abundance and counts across all transcripts per gene, weighted by effective length.
4. Extract and export three gene-level matrices: counts (read assignments), abundance (TPM), and length (effective transcript length per sample).
5. Validation: confirm that output matrices have correct dimensions (genes × samples), contain non-negative numeric values, and match expected gene IDs from tx2gene mapping.
6. References: source article (DOI: 10.12688/f1000research.7563.1)

## Workflow Ports

**Inputs:**

- `salmon_quant_files` — Six salmon quant.sf.gz quantification files
- `tx2gene_mapping` — tx2gene.gencode.v27.csv transcript-to-gene mapping

**Outputs:**

- `gene_counts_matrix` — Gene-level counts matrix
- `gene_abundance_matrix` — Gene-level abundance (TPM) matrix
- `gene_length_matrix` — Gene-level length matrix

## Provenance

- **Source kind:** github
- **Synthesized from:** `github:thelovelab__tximport`
- **Synthesized at:** 2026-06-15T19:10:33+00:00

---

*Card produced by **AgenticScienceBuilder (ASB)** — heuristic + LLM-assisted extraction from a research artifact. See the `ro-crate-metadata.json` in this capsule for full provenance.*
