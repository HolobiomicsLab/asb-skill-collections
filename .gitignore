# Python
__pycache__/
*.py[cod]
*.pyo
*.egg
*.egg-info/
dist/
build/
.eggs/
.env
.venv/
venv/
env/
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.cover
*.log
*.pot
*.mo

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE
.idea/
.vscode/
*.swp
*~

# Data / generated artefacts
outputs/
*.jsonl.gz
*.parquet
.license-cache.json

# License clarification wave output (runtime artifact — not committed)
governance/license_clarification/wave.yaml
.license-cache-v2.json

# Local resolver caches (preprint licence lookups, embeddings). Never committed.
# Leaf embedding caches are large and regenerated from source / shipped as
# Zenodo + GitHub Release assets.
.cache/
**/.cache/
*.npz

# curate.py writes a local audit snapshot; it is a dev-tool output, not a
# release artifact like gate_report.json.
collections/*/v*/curation_report.json
